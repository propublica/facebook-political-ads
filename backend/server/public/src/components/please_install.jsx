import React from "react";

const PleaseInstall = () =>
  true ? (
    <div>
      Hey, we're glad you're here! (this is displayed only conditionally)
    </div>
  ) : null;

export default PleaseInstall;
